{
  "name": "xander-backend-functions",
  "description": "Backend Functions for Xander Global Scholars",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-graphql": "^0.12.0",
    "firebase-admin": "^13.0.1",
    "firebase-functions": "^4.5.1",
    "graphql": "^15.9.0",
    "helmet": "^7.1.0",
    "mongoose": "^7.6.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "nodemon": "^3.1.7",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": "18"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
